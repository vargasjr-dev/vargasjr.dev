# Contributing to VargasJR.dev

## Development

```bash
npm install
npx next dev
```

## Style Guide

- Static imports at the top of files
- Run `npx next lint` and `npx prettier --check .` before committing
- All tests: `npx vitest`

## Pull Requests

- Descriptive branch names
- Clear commit messages
- CI checks must pass
